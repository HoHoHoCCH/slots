# Slots

Save slots for coding projects.

# todolist


README is not enough for v1. It needs install, commands, examples, and a warning that this is not a replacement for git.

If a saved file is missing during load, you currently warn and continue. For v1, I’d probably stop loading instead to avoid partial restores.
Add slots delete <name> eventually.
